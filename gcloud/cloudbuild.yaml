# $ gcloud container builds submit . --config=cloudbuild.yaml

steps:
- name: 'gcr.io/cloud-builders/mvn'
  args: ['-U', '-DskipTests', '-B', 'clean', 'install']

- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/search-service:$REVISION_ID', '.']

#- name: 'gcr.io/cloud-builders/kubectl'
#  args: ['set', 'image', 'deployment/search-deployment', 'search=gcr.io/$PROJECT_ID/search-service:$REVISION_ID' ]

images: ['gcr.io/$PROJECT_ID/search-service:$REVISION_ID']

